# Nug-Tracker

Vercel-ready React + Vite + TypeScript scaffold for the Nug-Tracker app.

## Start

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

This version is local-first and uses browser storage through a storage adapter.
Supabase can be added later without rewriting the screens.
